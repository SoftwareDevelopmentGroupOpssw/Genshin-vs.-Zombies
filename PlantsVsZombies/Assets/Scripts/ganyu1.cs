using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ganyu1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      //  GameObject.Instantiate(arrow);
		
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
